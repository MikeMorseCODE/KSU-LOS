name: OnePlus 11 GKI Kernel Build + WildKSU + SUSFS + Release

on:
  workflow_dispatch:
    inputs:
      lineage_build_url:
        description: Optional LineageOS full build URL used to extract boot.img
        required: false
        type: string
  schedule:
    - cron: '0 4 * * *'

jobs:
  build_kernel:
    runs-on: ubuntu-latest
    env:
      ARCH: arm64
      CROSS_COMPILE: aarch64-linux-gnu-
      CLANG_DIR: ${{ github.workspace }}/prebuilts/clang/host/linux-x86/clang-r563880c/bin
      LLVM_BINUTILS_DIR: ${{ github.workspace }}/prebuilts/clang/host/linux-x86/llvm-binutils-stable
      DEFCONFIG: lineage_oneplus11_defconfig
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      KERNEL_OUT: out

    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bison flex libssl-dev bc ccache git wget unzip python3 python3-pip zip curl squashfs-tools abootimg

      - name: Fetch Android Clang toolchain
        run: |
          mkdir -p prebuilts/clang/host
          git clone --depth 1 --branch lineage-23.2 https://github.com/LineageOS/android_prebuilts_clang_host_linux-x86.git prebuilts/clang/host/linux-x86
          echo "$CLANG_DIR" >> $GITHUB_PATH
          echo "$LLVM_BINUTILS_DIR" >> $GITHUB_PATH

      - name: Setup ccache
        run: |
          mkdir -p $CCACHE_DIR
          ccache -M 10G
          ccache -s

      - name: Detect CPU cores
        run: echo "JOBS=$(nproc)" >> $GITHUB_ENV

      - name: Clone LineageOS kernel repos
        run: |
          git clone --depth 1 --branch lineage-23.2 https://github.com/LineageOS/android_kernel_oneplus_sm8550.git kernel/oneplus/sm8550
          git clone --depth 1 --branch lineage-23.2 https://github.com/LineageOS/android_kernel_oneplus_sm8550-devicetrees.git kernel/oneplus/sm8550-devicetrees
          git clone --depth 1 --branch lineage-23.2 https://github.com/LineageOS/android_kernel_oneplus_sm8550-modules.git kernel/oneplus/sm8550-modules
          ln -sfn ../sm8550-modules kernel/oneplus/sm8550-modules

      - name: Detect kernel version
        id: detect_kernel
        run: |
          KVER=$(grep "^VERSION =" kernel/oneplus/sm8550/Makefile | awk '{print $3}')
          KPATCH=$(grep "^PATCHLEVEL =" kernel/oneplus/sm8550/Makefile | awk '{print $3}')
          echo "Kernel version: $KVER.$KPATCH"
          echo "KERNEL_VERSION=$KVER.$KPATCH" >> $GITHUB_ENV

      - name: Decompress config.gz if exists
        run: |
          CONFIG_PATH=kernel/oneplus/sm8550/arch/arm64/configs/config.gz
          DEFCONFIG_PATH=kernel/oneplus/sm8550/arch/arm64/configs/lineage_oneplus11_defconfig
          if [ -f "$CONFIG_PATH" ]; then
            gunzip -c "$CONFIG_PATH" > "$DEFCONFIG_PATH"
          else
            echo "config.gz not found, assuming lineage_oneplus11_defconfig exists"
          fi

      - name: Setup KernelSU-Next
        working-directory: kernel/oneplus/sm8550
        run: |
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s next
          echo "CONFIG_KSU=y" >> arch/arm64/configs/lineage_oneplus11_defconfig

      - name: Clone SUSFS repo
        run: |
          git clone --depth 1 -b gki-android13-5.15.202 https://github.com/simonpunk/susfs4ksu.git susfs4ksu

      - name: Apply SUSFS patches
        working-directory: kernel/oneplus/sm8550
        run: |
          cp ../../susfs4ksu/kernel_patches/fs/* fs/
          cp ../../susfs4ksu/kernel_patches/include/linux/* include/linux/
          SUSFS_PATCH=$(ls ../../susfs4ksu/kernel_patches/*.patch 2>/dev/null | head -1)
          if [ -n "$SUSFS_PATCH" ]; then
            patch -p1 < "$SUSFS_PATCH" && echo "Applied $(basename $SUSFS_PATCH)"
          fi
          KSU_SUSFS_PATCH=$(find KernelSU-Next -name "*susfs*.patch" 2>/dev/null | head -1)
          [ -n "$KSU_SUSFS_PATCH" ] && patch -p1 < "$KSU_SUSFS_PATCH" || true
          cat >> arch/arm64/configs/lineage_oneplus11_defconfig << 'EOF'
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          EOF

      - name: Clone and apply WildKernels patches
        working-directory: kernel/oneplus/sm8550
        run: |
          git clone --depth 1 https://github.com/WildKernels/kernel_patches.git ../../wildkernels_patches
          for patch in ../../wildkernels_patches/*.patch; do
            [ -f "$patch" ] || continue
            if git apply --check "$patch" 2>/dev/null; then
              git apply "$patch" && echo "Applied $(basename $patch)"
            else
              echo "Skipping $(basename $patch)"
            fi
          done

      - name: Resolve boot image source
        run: |
          if [ -n "${{ github.event.inputs.lineage_build_url }}" ]; then
            echo "LINEAGE_BUILD_URL=${{ github.event.inputs.lineage_build_url }}" >> $GITHUB_ENV
          else
            echo "LINEAGE_BUILD_URL=https://mirrorbits.lineageos.org/full/salami/20260417/lineage-23.2-20260417-nightly-salami-signed.zip" >> $GITHUB_ENV
          fi

      - name: Download LineageOS full build
        run: |
          wget -q "$LINEAGE_BUILD_URL" -O lineage.zip
          unzip -q lineage.zip boot.img system.img
          mkdir -p kernel/oneplus/sm8550/ramdisk

      - name: Extract ramdisk.img using abootimg
        working-directory: kernel/oneplus/sm8550/ramdisk
        run: abootimg -x ../../../boot.img

      - name: Build kernel
        working-directory: kernel/oneplus/sm8550
        run: |
          export PATH="$CLANG_DIR:$LLVM_BINUTILS_DIR:$PATH"
          make O=$KERNEL_OUT LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu- $DEFCONFIG
          make -j$JOBS O=$KERNEL_OUT LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu- Image.gz-dtb

      - name: Compile modules
        working-directory: kernel/oneplus/sm8550-modules
        run: |
          export PATH="$CLANG_DIR:$LLVM_BINUTILS_DIR:$PATH"
          make -C ../sm8550 M=$(pwd) O=../sm8550/$KERNEL_OUT LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu- modules

      - name: Package boot.img using mkbootimg.py
        working-directory: kernel/oneplus/sm8550
        run: |
          mkdir -p $KERNEL_OUT/bootimg
          cp $KERNEL_OUT/arch/arm64/boot/Image.gz-dtb $KERNEL_OUT/bootimg/zImage-dtb
          wget -q https://android.googlesource.com/platform/system/tools/mkbootimg/+/refs/heads/master/mkbootimg.py?format=TEXT -O mkbootimg.py.base64
          base64 --decode mkbootimg.py.base64 > mkbootimg.py
          chmod +x mkbootimg.py
          python3 mkbootimg.py --kernel $KERNEL_OUT/bootimg/zImage-dtb --ramdisk ramdisk/boot.img-ramdisk.cpio.gz --output $KERNEL_OUT/boot.img

      - name: Create minimal AK3 flashable zip
        working-directory: kernel/oneplus/sm8550
        run: |
          mkdir -p $KERNEL_OUT/flashable-AK3/META-INF/com/google/android
          cp $KERNEL_OUT/boot.img $KERNEL_OUT/flashable-AK3/boot.img
          echo "ui_print(\"Flashing OnePlus11 Kernel\");" > $KERNEL_OUT/flashable-AK3/META-INF/com/google/android/updater-script
          echo "package_extract_file(\"boot.img\", \"/boot.img\");" >> $KERNEL_OUT/flashable-AK3/META-INF/com/google/android/updater-script
          cd $KERNEL_OUT/flashable-AK3
          zip -r OnePlus11-Kernel-AK3.zip *

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OnePlus11-Kernel-Images
          path: |
            kernel/oneplus/sm8550/out/boot.img
            kernel/oneplus/sm8550/out/flashable-AK3/OnePlus11-Kernel-AK3.zip

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "GKI-23.2-${{ github.run_number }}"
          name: "OnePlus11 Kernel Build #${{ github.run_number }}"
          body: "Automated OnePlus 11 GKI kernel build with WildKSU + SUSFS. Boot.img + AK3 zip."
          draft: false
          prerelease: false
          files: |
            kernel/oneplus/sm8550/out/boot.img
            kernel/oneplus/sm8550/out/flashable-AK3/OnePlus11-Kernel-AK3.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
